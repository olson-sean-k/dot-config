colorscheme catppuccin-mocha
set background=dark

" TODO: Setting colors here seems to have no effect. The Lua plugin provides
"       customization hooks. Perhaps the implementation interferes with this and
"       those hooks must be used instead?
let g:cursor_hold_search_group_attrs = 'term=reverse gui=reverse'

let g:lightline['colorscheme'] = 'catppuccin'
