add_newline = false
format = """\
    $username\
    $hostname\
    $directory\
    $git_branch\
    $git_status\
    $package\
    $line_break\
    $jobs\
    $character\
    """

[username]
format = "[$user]($style) on "
show_always = true
